# Настройка бекенда и Telegram бота

## Шаг 1: Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "Психотерапевт Админ")
4. Введите username бота (например: "psyproject_admin_bot")
5. Скопируйте токен бота (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Получение вашего Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте `/start`
3. Скопируйте ваш ID (число, например: `123456789`)

## Шаг 3: Установка бекенда

```bash
# Перейдите в папку backend
cd backend

# Установите зависимости
npm install

# Создайте файл .env
cp .env.example .env
```

## Шаг 4: Настройка .env файла

Откройте файл `backend/.env` и заполните:

```env
PORT=3000
NODE_ENV=development

# Вставьте токен вашего бота
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Вставьте ваш Telegram ID
TELEGRAM_ADMIN_ID=123456789

DATABASE_PATH=./database.sqlite

# Для локальной разработки
ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080
```

## Шаг 5: Запуск бекенда

```bash
# Development режим (с автоперезагрузкой)
npm run dev

# Или обычный запуск
npm start
```

Вы должны увидеть:
```
🚀 Server running on port 3000
📱 Telegram bot is active
🗄️  Database initialized
✅ Telegram bot initialized
✅ Database initialized
```

## Шаг 6: Тестирование бота

1. Откройте вашего бота в Telegram (по username который вы создали)
2. Отправьте `/start`
3. Бот должен ответить приветственным сообщением

## Шаг 7: Тестирование API

Откройте новый терминал и выполните:

```bash
# Тест создания заявки
curl -X POST http://localhost:3000/api/applications \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тест Тестов",
    "phone": "+79991234567",
    "email": "test@example.com",
    "requestType": "urgent",
    "message": "Тестовая заявка"
  }'
```

Вы должны:
1. Получить ответ `{"success":true,...}`
2. Получить уведомление в Telegram боте

## Шаг 8: Обновление фронтенда

Файл `consultation-popup.js` уже обновлен для работы с API.

Для локальной разработки откройте сайт через локальный сервер:

```bash
# Если у вас Python 3
python3 -m http.server 8080

# Или используйте любой другой локальный сервер
# npx serve
# php -S localhost:8080
```

Откройте http://localhost:8080 и протестируйте форму.

## Команды бота

После запуска бота доступны команды:

- `/start` - Начать работу
- `/stats` - Статистика заявок
- `/new` - Показать новые заявки
- `/help` - Справка

## Деплой на production

### Вариант 1: VPS (Рекомендуется)

1. Загрузите код на сервер
2. Установите Node.js и PM2
3. Настройте .env с production данными
4. Запустите через PM2:

```bash
pm2 start server.js --name psyproject-backend
pm2 save
pm2 startup
```

### Вариант 2: Heroku

```bash
heroku create your-app-name
heroku config:set TELEGRAM_BOT_TOKEN=your_token
heroku config:set TELEGRAM_ADMIN_ID=your_id
heroku config:set ALLOWED_ORIGINS=https://yourdomain.com
git push heroku main
```

### Вариант 3: Railway.app

1. Зарегистрируйтесь на railway.app
2. Создайте новый проект из GitHub
3. Добавьте переменные окружения
4. Деплой произойдет автоматически

## Обновление URL API на фронтенде

После деплоя обновите URL в `consultation-popup.js`:

```javascript
const API_URL = window.location.hostname === 'localhost' 
    ? 'http://localhost:3000/api' 
    : 'https://your-backend-url.com/api'; // <-- Замените на ваш URL
```

## Troubleshooting

### Бот не отвечает
- Проверьте правильность токена в .env
- Убедитесь что бот запущен (npm run dev)
- Проверьте логи в консоли

### Не приходят уведомления
- Проверьте правильность TELEGRAM_ADMIN_ID
- Убедитесь что вы написали боту /start
- Проверьте логи на ошибки

### CORS ошибки
- Добавьте URL вашего сайта в ALLOWED_ORIGINS
- Для локальной разработки добавьте http://localhost:8080

### База данных не создается
- Проверьте права на запись в папке backend
- Убедитесь что путь DATABASE_PATH корректный

## Следующие шаги

- [ ] Настроить домен и SSL сертификат
- [ ] Добавить мониторинг (UptimeRobot)
- [ ] Настроить бэкапы базы данных
- [ ] Добавить Telegram Web App для управления специалистами
- [ ] Интегрировать с календарем для записи
