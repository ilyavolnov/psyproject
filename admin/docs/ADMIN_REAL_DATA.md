# 📊 Реальные данные в админке

## ✅ Что изменено:

### Удалены тестовые данные
- ❌ Удалены все mock данные из `admin-panel.js`
- ❌ Удалены статичные массивы `allRequests`
- ❌ Удалены функции `generateRecentRequests()` и `generateAllRequests()`

### Добавлена работа с реальным API

#### 📊 Дашборд
- Загружает реальную статистику из `/api/requests/stats/summary`
- Показывает последние 5 заявок из базы данных
- Обновляется при каждом открытии

#### 📋 Заявки
- Загружает все заявки из `/api/requests`
- Фильтрация работает через API
- Вкладки (Все/Архив/Удаленное) загружают данные из БД
- Поиск и фильтры отправляют запросы к API

#### 🆕 Новые функции

**1. Создание тестовой заявки**
```javascript
window.createTestRequest()
```
- Кнопка "🧪 Создать тестовую заявку" в разделе Заявки
- Генерирует случайные данные
- Сохраняет в базу данных через API
- Автоматически обновляет список

**2. Сохранение изменений заявки**
```javascript
window.saveRequestChanges(requestId)
```
- Изменение статуса заявки
- Назначение специалиста
- Сохранение в БД

**3. Удаление заявки**
```javascript
window.deleteRequest(requestId)
```
- Мягкое удаление (soft delete)
- Заявка помечается как удаленная
- Можно восстановить из вкладки "Удаленное"

**4. Обновление badge**
```javascript
window.updateRequestsBadge()
```
- Показывает количество новых заявок
- Обновляется автоматически при загрузке

## 🔄 API Endpoints используемые

### Статистика
```
GET /api/requests/stats/summary
```
Возвращает:
```json
{
  "success": true,
  "data": {
    "total": 10,
    "new": 3,
    "pending": 5,
    "completed": 2,
    "archived": 0,
    "deleted": 0
  }
}
```

### Список заявок
```
GET /api/requests?tab=all&status=new&type=consultation
```
Параметры:
- `tab` - all/archive/deleted
- `status` - new/pending/completed
- `type` - consultation/specialist/certificate/course/supervision
- `specialistId` - ID специалиста
- `limit` - количество записей
- `offset` - смещение для пагинации

### Одна заявка
```
GET /api/requests/:id
```

### Создание заявки
```
POST /api/requests
```
Body:
```json
{
  "name": "Имя",
  "phone": "+7 (999) 123-45-67",
  "email": "email@example.com",
  "request_type": "consultation",
  "message": "Сообщение",
  "specialist_id": 1
}
```

### Обновление заявки
```
PUT /api/requests/:id
```
Body:
```json
{
  "status": "pending",
  "specialist_id": 2
}
```

### Удаление заявки
```
DELETE /api/requests/:id
```

## 🎯 Как использовать

### Создать тестовую заявку
1. Откройте админку
2. Перейдите в раздел "Заявки"
3. Нажмите "🧪 Создать тестовую заявку"
4. Заявка появится в списке

### Редактировать заявку
1. Нажмите "Открыть" на заявке
2. Измените статус или специалиста
3. Нажмите "💾 Сохранить изменения"

### Фильтровать заявки
1. Используйте выпадающие списки вверху
2. Выберите статус, тип или специалиста
3. Список обновится автоматически

### Переключать вкладки
1. Нажмите на вкладку (Все/Архив/Удаленное)
2. Загрузятся соответствующие заявки

## 📝 Формат данных

### Заявка в БД
```javascript
{
  id: 1,
  name: "Имя Фамилия",
  phone: "+7 (999) 123-45-67",
  email: "email@example.com",
  request_type: "consultation",
  message: "Текст сообщения",
  specialist_id: 1,
  course_id: null,
  certificate_amount: null,
  status: "new",
  archived: 0,
  deleted: 0,
  created_at: "2025-11-13T20:00:00.000Z",
  updated_at: "2025-11-13T20:00:00.000Z"
}
```

### Типы заявок
- `consultation` - Общая консультация
- `specialist` - Запись к специалисту
- `certificate` - Покупка сертификата
- `course` - Покупка курса
- `supervision` - Супервизия
- `urgent` - Срочная консультация
- `family` - Семейная сессия

### Статусы
- `new` - Новая (🆕)
- `pending` - В обработке (⏳)
- `completed` - Завершена (✅)

## 🔧 Технические детали

### Форматирование даты
```javascript
function formatDate(dateString) {
    const date = new Date(dateString);
    return date.toLocaleString('ru-RU', {
        day: '2-digit',
        month: '2-digit',
        year: 'numeric',
        hour: '2-digit',
        minute: '2-digit'
    });
}
```

### Генерация тестовых данных
```javascript
const testData = {
    name: 'Тестовый клиент ' + Date.now(),
    phone: '+7 (999) XXX-XX-XX', // случайный
    email: 'test' + Date.now() + '@example.com',
    request_type: // случайный тип
    message: 'Это тестовая заявка',
    specialist_id: // случайный 1-5
};
```

## 🐛 Troubleshooting

### Заявки не загружаются
1. Проверьте, что Backend запущен на порту 3001
2. Откройте консоль браузера (F12)
3. Проверьте ошибки в Network tab

### Тестовая заявка не создается
1. Убедитесь, что Backend работает
2. Проверьте логи Backend в терминале
3. Проверьте, что база данных инициализирована

### Изменения не сохраняются
1. Проверьте консоль на ошибки
2. Убедитесь, что API endpoint доступен
3. Проверьте формат отправляемых данных

---

**Готово! Админка теперь работает с реальными данными из базы! 🎉**
