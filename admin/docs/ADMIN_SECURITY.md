# 🔐 Безопасность админ-панели

## Реализованные меры безопасности

### 1. **Хеширование паролей (SHA-256)**
- Пароли никогда не хранятся в открытом виде
- Используется криптографически стойкий алгоритм SHA-256
- Хеширование происходит на клиенте перед отправкой

### 2. **Защита от брутфорса**
- Максимум 5 попыток входа
- Блокировка аккаунта на 15 минут после превышения лимита
- Автоматический сброс счетчика через 5 минут бездействия
- Отображение оставшихся попыток

### 3. **Безопасные токены сессии**
- Генерация криптографически стойких токенов (256 бит)
- Использование `crypto.getRandomValues()` (как в крипто-кошельках)
- Токены хешируются перед сохранением
- Автоматическое истечение сессии через 24 часа

### 4. **Защита от timing attacks**
- Добавлена случайная задержка при проверке учетных данных
- Предотвращает определение существования пользователя по времени ответа

### 5. **Валидация сессий**
- Проверка срока действия токена
- Автоматическое удаление истекших сессий
- Защита от подделки токенов

## Как установить новый пароль

### Шаг 1: Генерация хеша
1. Откройте `generate-password-hash.html` в браузере
2. Введите желаемый пароль
3. Нажмите "Сгенерировать SHA-256 хеш"
4. Скопируйте полученный хеш

### Шаг 2: Обновление credentials
Откройте `admin-login.js` и найдите объект `validCredentials`:

```javascript
const validCredentials = {
    'admin': 'ВАШ_ХЕШ_ЗДЕСЬ',
    'margarita': 'ВАШ_ХЕШ_ЗДЕСЬ'
};
```

### Шаг 3: Удалите генератор
⚠️ **ВАЖНО**: После генерации хешей удалите файл `generate-password-hash.html`!

## Текущие учетные данные (DEMO)

```
Логин: admin
Пароль: admin
Хеш: 8c6976e5b5410415bde908bd4dee15dfb167a9c873fc4bb8a81f6f2ab448a918

Логин: margarita  
Пароль: password
Хеш: 5e884898da28047151d0e56f8dc6292773603d0d6aabbdd62a11ef721d1542d8
```

## Рекомендации по безопасности

### ✅ Обязательно:
1. **Смените пароли по умолчанию** сразу после установки
2. **Используйте сложные пароли** (минимум 16 символов, буквы, цифры, спецсимволы)
3. **Удалите `generate-password-hash.html`** после генерации хешей
4. **Не храните пароли в открытом виде** нигде в коде
5. **Регулярно меняйте пароли** (раз в 3-6 месяцев)

### 🔒 Дополнительная защита:
1. Ограничьте доступ к `/admin-*` файлам через `.htaccess` или nginx
2. Используйте HTTPS для всех админ-страниц
3. Настройте IP whitelist для админ-панели
4. Включите двухфакторную аутентификацию (2FA)
5. Регулярно проверяйте логи входов

### 🚫 Никогда не делайте:
1. Не используйте простые пароли типа "admin", "123456"
2. Не храните пароли в коде или комментариях
3. Не отключайте защиту от брутфорса
4. Не делитесь учетными данными
5. Не оставляйте генератор хешей на продакшене

## Структура безопасности

```
Уровень 1: Хеширование паролей (SHA-256)
    ↓
Уровень 2: Защита от брутфорса (5 попыток / 15 мин блокировка)
    ↓
Уровень 3: Безопасные токены сессии (256-bit random)
    ↓
Уровень 4: Валидация и истечение сессий (24 часа)
    ↓
Уровень 5: Защита от timing attacks
```

## Мониторинг безопасности

Регулярно проверяйте:
- Количество неудачных попыток входа
- Активные сессии
- Подозрительную активность
- Истекшие токены

## Восстановление доступа

Если вы забыли пароль:
1. Откройте `generate-password-hash.html`
2. Сгенерируйте новый хеш
3. Обновите `admin-login.js`
4. Очистите localStorage в браузере
5. Войдите с новым паролем

## Контакты

При обнаружении уязвимостей или вопросах по безопасности:
- Email: security@example.com

---

**Последнее обновление:** 2024
**Версия безопасности:** 1.0
